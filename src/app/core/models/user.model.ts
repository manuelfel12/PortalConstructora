export interface UserToken {
	// active_status: boolean;
	// expiration: string;
	// is_internal: boolean;
	// refresh_token: string;
	// sex: string;
	token: string;
// 	username: string;
// 	identificationNumber: string;
 }
